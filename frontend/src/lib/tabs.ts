// The tab order, shared by the bar and the swipe gesture so the two can never
// disagree about what is left and what is right.
export const TAB_HREFS = [
  "/marks",
  "/attendance",
  "/dashboard",
  "/timetable",
  "/calendar",
] as const;
